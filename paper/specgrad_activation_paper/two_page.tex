\documentclass[10pt]{article}

\usepackage[a4paper,margin=0.68in]{geometry}
\usepackage{amsmath,amssymb,amsfonts,booktabs,array}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{multicol}

\setlength{\columnsep}{0.24in}
\setlength{\parskip}{0.6pt}
\setlength{\parindent}{0pt}
\setlength{\textfloatsep}{6pt plus 1pt minus 1pt}
\setlength{\floatsep}{6pt plus 1pt minus 1pt}
\setlength{\intextsep}{6pt plus 1pt minus 1pt}
\setlength{\abovedisplayskip}{3.5pt plus 1pt minus 1pt}
\setlength{\belowdisplayskip}{3.5pt plus 1pt minus 1pt}
\setlength{\abovedisplayshortskip}{3pt plus 1pt minus 1pt}
\setlength{\belowdisplayshortskip}{3pt plus 1pt minus 1pt}
\emergencystretch=2em

\newcommand{\R}{\mathbb{R}}
\newcommand{\Loss}{\mathcal L}
\newcommand{\I}{\mathcal I}
\newcommand{\op}{\mathrm{op}}
\newcommand{\norm}[1]{\left\lVert #1 \right\rVert}
\newcommand{\inner}[2]{\left\langle #1,#2 \right\rangle}
\newcommand{\srank}{\operatorname{srank}}
\newcommand{\nrank}{\operatorname{nrank}}
\newcommand{\ssrank}{\operatorname{ssrank}}
\newcommand{\vecop}{\operatorname{vec}}
\newcommand{\polar}{\operatorname{polar}}
\input{tables/e11_paper_numbers.tex}

\title{\vspace{-1.2em}Head-to-Tail Interference in Long-Tailed Small-Batch Training}
\author{\small Tianyang Liu\\[-0.15em]\small UCDavis\\[-0.15em]\small \texttt{tlyliu@ucdavis.edu}}
\date{\vspace{-1.4em}}

\begin{document}
\maketitle
\vspace{-1.1em}
\begin{multicols}{2}

\section*{Abstract}
Long-tailed small-batch training can update a model on frequent head classes while rare tail classes are absent. This report studies a local stability question: after two update directions make the same first-order progress on the head batch, which one causes less squared logit drift on held-out tail examples? We introduce a matched-head-gain drift diagnostic and a head-to-tail interference coefficient. For a matrix block with tail perturbation \(J_T(D)=B_TDA_T\), the analysis predicts that spectral/polar geometry has a smaller worst-case drift bound when \(\nrank(G_H)>\ssrank(B_T,A_T)\). Controlled boundary, long-tailed digits, and CIFAR-100-LT ResNet18 diagnostics support this local drift mechanism, while also showing that lower logit drift does not automatically imply better tail loss or accuracy.

\section*{1. Problem}
Long-tailed small-batch training has a local function-drift failure mode: frequent head-class updates can change logits on rare tail examples before tail samples reappear. Ordinary loss-decrease comparisons do not isolate this effect, because an update may look stable simply by moving less. We therefore use the comparison
\[
\boxed{\text{match head progress, then compare tail drift}.}
\]
This is a mechanism claim rather than an optimizer-level benchmark claim: after different update geometries obtain the same first-order head-batch gain, compare how much they perturb a held-out tail function. Tail-example logit drift always means drift of the full class-logit vector on tail examples, not only tail-class logits.

\section*{2. Matched-Gain Geometry}
Let \(g_H=\nabla_\theta\Loss_H(\theta)\) be the head-batch gradient and let \(F_T(\theta)\) be the model output on a held-out tail batch. Under a local linearization,
\[
F_T(\theta+\Delta)-F_T(\theta)\approx J_T\Delta .
\]
We compare updates only after fixing the same head gain,
\[
-\inner{g_H}{\Delta}=\rho,
\]
and then measure tail drift \(\norm{J_T\Delta}\). This removes the confound that an update may look tail-safe only because it moved less and made less head progress.

For a parameter norm \(N\), define the tail sensitivity and head-to-tail interference coefficient
\[
K_{T,N}=\sup_{\norm{\Delta}_N\le 1}\norm{J_T\Delta},
\qquad
\I_N(T\mid H)=\frac{K_{T,N}^2}{\norm{g_H}_{N,*}^2}.
\]
If \(\Delta_N\) is the norm-\(N\) steepest direction scaled to head gain \(\rho\), then
\[
\norm{J_T\Delta_N}^2\le \rho^2\I_N(T\mid H).
\]
Thus the relevant quantity is not raw loss decrease, but tail drift after head progress has been matched.
The worst-case coefficient \(\I_N\) can be separated from the realized actual-direction quantity \(\widetilde{\I}_N=\norm{J_Tu_N}^2/\norm{g_H}_{N,*}^2\), which is what direct drift measurements estimate after scaling.
For non-steepest Muon-style directions, we instead use the arbitrary-direction matched-gain coefficient
\[
\Delta_d=-\rho d/\inner{g_H}{d},
\]
\[
C(d;T\mid H)=\norm{J_Td}^2/\inner{g_H}{d}^2,
\]
with \(\inner{g_H}{d}>0\). Thus \(\polar(M_t)\) and NS\((M_t)\) are normalized by their actual head alignment, not by a dual norm for the current gradient.

\section*{3. Matrix-Block Condition}
For a matrix block \(W\), write the tail perturbation as
\[
J_T(D)=B_TDA_T,
\]
where \(A_T\) is the tail activation entering the layer and \(B_T\) is the downstream tail Jacobian. Frobenius geometry gives
\[
\I_F=
\frac{\norm{B_T}_{\op}^2\norm{A_T}_{\op}^2}{\norm{G_H}_F^2},
\]
while spectral geometry gives
\[
\I_S=
\frac{\sum_i\sigma_i(B_T)^2\sigma_i(A_T)^2}{\norm{G_H}_*^2}.
\]
Therefore spectral geometry has a smaller worst-case matched-gain drift bound when
\[
\boxed{\nrank(G_H)>\ssrank(B_T,A_T).}
\]
Equivalently, \(\I_S/\I_F=\ssrank(B_T,A_T)/\nrank(G_H)\).
Here
\[
\ssrank(B_T,A_T)=
\frac{\sum_i\sigma_i(B_T)^2\sigma_i(A_T)^2}
{\sigma_1(B_T)^2\sigma_1(A_T)^2}.
\]
The left side measures spectral spread in the head gradient; the right side measures downstream-aware tail sensitivity, not only activation stable rank.
This is a sandwich block condition. In a general nonlinear layer one should replace it with the vectorized operator form \(\vecop(\mathcal{J}_{T,\ell}(D))=L_{T,\ell}\vecop(D)\); the displayed singular-value formula is the special case \(L_{T,\ell}=A_T^\top\otimes B_T\).

\section*{4. Evidence}
Digits diagnostics use the scikit-learn \(8\times 8\) digits data with pixels scaled to the unit interval, a bias-free two-layer MLP with hidden dimension 32, fixed head classes \(0\)--\(4\), fixed tail classes \(5\)--\(9\), 20 random within-class splits, noisy mini-batch warmup, matched first-order gain \(\rho=0.02\Loss_H(\theta)\), and paired log-scale CIs for the tested split/seed distribution, not generalization guarantees. The MLP spectral direction is blockwise: \(d_S=\{\polar(G_\ell)\}_{\ell=1}^2\), then globally scaled by \(\Delta_d=-\rho d/\inner{g_H}{d}\). The largest local-linearization relative-error CI upper endpoint is \(\EelevenLocalLinearizationMaxRelativeErrorCiHigh\).

\begin{sloppypar}
The controlled boundary is directionally correct: the synthetic positive case has squared drift ratio \(\EelevenHeadTailPositiveDriftRatio\), while the reversed case has squared drift ratio \(\EelevenHeadTailNegativeDriftRatio\). The controlled long-tailed digits diagnostic gives lower drift but not a direct performance claim: the one-step squared tail-example logit drift ratio is \(\EelevenLongTailOneStepDriftRatio ~\EelevenLongTailOneStepDriftRatioCi\), and the CE tail-loss difference is \(\EelevenLongTailOneStepTailLossDiff ~\EelevenLongTailOneStepTailLossDiffCi\). A GPU CIFAR-100-LT ResNet18 check gives squared drift ratio \(\EelevenCifarResNetOneStepDriftRatio ~\EelevenCifarResNetOneStepDriftRatioCi\), with a smaller-head-gain check at \(\EelevenCifarResNetRhoZeroZeroTwoDriftRatio ~\EelevenCifarResNetRhoZeroZeroTwoDriftRatioCi\), checkpoint-sweep worst ratio \(\EelevenCifarResNetCheckpointSweepWorstDriftRatio ~\EelevenCifarResNetCheckpointSweepWorstDriftRatioCi\), final-layer condition worst ratio \(\EelevenCifarResNetFcConditionWorstDriftRatio ~\EelevenCifarResNetFcConditionWorstDriftRatioCi\), tail-quality-control worst ratio \(\EelevenCifarResNetTailQualityWorstDriftRatio ~\EelevenCifarResNetTailQualityWorstDriftRatioCi\), and all-layer ResNet JVP observed ratio \(\EelevenCifarResNetLayerJvpObservedRatio ~\EelevenCifarResNetLayerJvpObservedRatioCi\). Head-alignment and norm ratios \(\EelevenLongTailOneStepAlignmentRatio\), \(\EelevenLongTailOneStepUpdateFroNormRatio\), and \(\EelevenLongTailOneStepUpdateOpNormRatio\) show that the scaling effect is norm-specific. The Muon-style checks are compatibility tests: fixed \(\polar(M_t)\) squared drift ratio \(\EelevenLongTailMuonBridgePolarMomentumDriftRatio\), trajectory NS\((M_t)\) ratio \(\EelevenLongTailPracticalMuonBridgeNsMomentumDriftRatio\), and Fro/GD-state NS\((M_t)\) ratio \(\EelevenLongTailMuonStateSourceControlFroNsMomentumDriftRatio\). Finally, unit-JVP ratios exceed \(1\), but scaled layerwise ratios are \(\EelevenLayerOneScaledJvpDriftRatio\) and \(\EelevenLayerTwoScaledJvpDriftRatio\), so matched-head-gain scaling is part of the mechanism.
\end{sloppypar}

{\raggedright
\section*{5. Interpretation}
The synthetic result shows that \(\ssrank(B_T,A_T)\) is an operative tail-side quantity: changing downstream-aware tail sensitivity changes the predicted drift ordering. The digits and CIFAR-100-LT ResNet18 one-step results give direct non-synthetic evidence for lower matched-gain tail-example logit drift, with squared drift ratios \(\EelevenLongTailOneStepDriftRatio ~\EelevenLongTailOneStepDriftRatioCi\) and \(\EelevenCifarResNetOneStepDriftRatio ~\EelevenCifarResNetOneStepDriftRatioCi\). The Muon-style direction compatibility check reports squared drift ratios compatible with the local polar mechanism for selected momentum and finite Newton--Schulz directions, but it does not explain full Muon training dynamics.

The layerwise diagnostic rules out an overly broad interpretation. Spectral/polar unit directions are not always less tail-sensitive; in the two MLP layers, the unit-direction JVP ratios exceed \(1\). The lower observed drift appears after matched-head-gain scaling, meaning that spectral/polar geometry can use a smaller operator-norm step despite a larger Frobenius-norm step in this diagnostic. The mechanism is therefore not ``spectral directions are always safer'', but ``spectral geometry can reduce measured tail drift after head progress is matched.''

\section*{6. Claim Boundary}
The supported claims are deliberately local. First, the sandwich theorem predicts a reversible boundary: the synthetic squared drift ratio changes from \(\EelevenHeadTailPositiveDriftRatio\) to \(\EelevenHeadTailNegativeDriftRatio\) when the rank condition is reversed, but the alignment ablation shows that this is a bound-ordering condition rather than a standalone realized-drift predictor. Second, the digits and CIFAR-100-LT ResNet18 one-step results support a squared tail-example logit drift claim, not a broad accuracy claim. Third, Muon-style momentum directions are selected-state compatibility checks for the local polar mechanism; finite Newton--Schulz approximation, momentum-gradient alignment, and state selection remain separate sources of variation. Fourth, layerwise evidence indicates that the lower observed drift comes from matched-head-gain scaling, not from uniformly lower unit-direction tail sensitivity.

The mechanism should be evaluated before making optimizer-level claims. If spectral/polar directions lower matched-gain tail drift but do not lower tail cross-entropy or improve tail accuracy, then the result is a stability mechanism rather than a performance theorem. A practical Muon claim would require showing that the same local geometry persists under momentum, approximation, schedules, weight decay, and checkpoint variation, and that the resulting trajectory improves standard tail metrics under tuned baselines.

\section*{7. Conclusion}
The current evidence supports a local mechanism claim: spectral/polar geometry can reduce matched-head-gain squared logit drift on tail examples when \(\nrank(G_H)\) is large relative to \(\ssrank(B_T,A_T)\), including in the CIFAR-100-LT ResNet18 one-step, checkpoint-sweep, tail-rich, and all-layer JVP checks. It does not establish optimizer-level performance or general long-tailed accuracy; the tail-rich ResNet control raises pre-update tail accuracy to \(\EelevenCifarResNetTailQualityBestTailAccuracy ~\EelevenCifarResNetTailQualityBestTailAccuracyCi\), but remains a local matched-gain diagnostic. A ResNet rank-side proxy scatter further shows why the downstream-aware term matters: mean gradient nuclear rank alone has positive correlation \(\EelevenCifarResNetConditionProxyRankPearson ~\EelevenCifarResNetConditionProxyRankPearsonCi\) with the log drift ratio across seed/checkpoint points. The classifier-layer condition check is closer to the theorem, and the all-layer JVP control extends the local bridge to every Conv/Linear matrix weight. Stronger claims require standard long-tailed benchmarks, practical Muon ablations, and held-out predictive condition tests.

\vspace{-0.15em}
{\scriptsize
\textbf{References.}
Davis and Drusvyatskiy, ``When Do Spectral Gradient Updates Help in Deep Learning?'', arXiv 2025.
Chen, Li, and Liu, ``Muon Optimizes Under Spectral Norm Constraints'', arXiv 2025.
Luo et al., ``Long-tailed Learning with Muon Optimizer'', OpenReview 2025.
Cui et al., ``Class-Balanced Loss Based on Effective Number of Samples'', CVPR 2019.
Kang et al., ``Decoupling Representation and Classifier for Long-Tailed Recognition'', ICLR 2020.
Pedregosa et al., ``Scikit-learn: Machine Learning in Python'', JMLR 2011.
}
\par}
\end{multicols}

\end{document}
