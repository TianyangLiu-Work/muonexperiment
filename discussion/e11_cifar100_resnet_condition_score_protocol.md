# E11 CIFAR-100 ResNet Condition-Score Protocol

This generated protocol pre-registers the next downstream-aware condition-score benchmark for the head-to-tail paper. It exists because the current checkpoint-transfer audit is a boundary result: source-observed drift and the early-layer prior transfer layer-risk ranking, but the legacy scaled-JVP score does not.

## Claim Boundary

Passing this protocol can support a predictive local condition-score claim for matched-head-gain tail-example drift. It cannot support a final long-tail accuracy claim, a practical Muon optimizer-performance claim, or a claim that the synthetic theorem is already a universal real-task predictor.

## Score Registry

| score_id                               | role                         | uses_observed_source_drift   | fit_rule                                                                                                                                                                        | input_features                                                                                                                          | heldout_claim_allowed          | reason                                                                                                                         |
|:---------------------------------------|:-----------------------------|:-----------------------------|:--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------|:-------------------------------|:-------------------------------------------------------------------------------------------------------------------------------|
| source_observed_drift_positive_control | positive_control             | yes                          | No fitting; ranks target layers by measured observed drift at the source checkpoint.                                                                                            | source checkpoint observed layer-only matched-head-gain drift                                                                           | no                             | This proves that layer-risk ordering is transferable, but it is not a standalone condition score.                              |
| early_layer_prior                      | architecture_prior_baseline  | no                           | No fitting; risk score is log(1 / layer_index).                                                                                                                                 | layer_index                                                                                                                             | baseline only                  | This is the strongest current simple non-condition baseline and must be beaten before claiming a useful condition score.       |
| legacy_scaled_jvp_ratio                | locked_boundary_baseline     | no                           | No fitting; use log scaled finite-difference JVP drift ratio from the source checkpoint.                                                                                        | geomean_scaled_jvp_tail_drift_sq_ratio_spectral_over_fro                                                                                | boundary only                  | The current audit shows this score preserves the below-one direction but fails raw and residual layer-risk ranking.            |
| condition_score_v2_calibrated_residual | primary_candidate            | calibration only             | Fit one ridge model on calibration splits only to predict source observed log drift after subtracting the early-layer baseline; freeze coefficients before held-out evaluation. | log_gradient_nuclear_rank; log_alignment_ratio; log_step_size_ratio; log_unit_jvp_ratio; log_scaled_jvp_ratio; log_tail_accuracy_before | yes, if all primary gates pass | This makes the next score explicitly downstream-aware while preventing target-checkpoint or target-architecture leakage.       |
| theory_sign_composite                  | secondary_zero_fit_candidate | no                           | No fitting; standardized log_gradient_nuclear_rank plus log_alignment_ratio minus log_scaled_jvp_ratio.                                                                         | mean_gradient_nuclear_rank; mean_alignment_ratio_spectral_over_fro; geomean_scaled_jvp_tail_drift_sq_ratio_spectral_over_fro            | secondary only                 | This keeps a zero-fit theorem-adjacent candidate beside the calibrated score; it cannot override the primary candidate result. |

## Split Registry

| split_id                                     | role                         | dataset                 | architecture                            | checkpoints                                       | seeds       | score_tuning_allowed   | target_used_for_tuning   | compute_mode      | planned_artifact_prefix                                                |
|:---------------------------------------------|:-----------------------------|:------------------------|:----------------------------------------|:--------------------------------------------------|:------------|:-----------------------|:-------------------------|:------------------|:-----------------------------------------------------------------------|
| legacy_resnet18_checkpoint_transfer_boundary | locked_retrospective_audit   | CIFAR-100-LT            | ResNet18 CIFAR stem                     | 2000/5000/10000 warmup steps                      | 10          | no                     | no                       | already generated | results/e11_cifar100_resnet_condition_score_audit                      |
| calibration_cifar100lt_resnet18_source_folds | calibration_only             | CIFAR-100-LT            | ResNet18 CIFAR stem                     | source folds drawn from training checkpoints only | at least 10 | yes                    | no                       | GPU via Slurm     | results/e11_cifar100_resnet_condition_score_next/calibration           |
| primary_heldout_checkpoint_resnet18          | primary_heldout_checkpoint   | CIFAR-100-LT            | ResNet18 CIFAR stem                     | held-out warmup steps not used for calibration    | at least 10 | no                     | no                       | GPU via Slurm     | results/e11_cifar100_resnet_condition_score_next/heldout_checkpoint    |
| primary_heldout_architecture_resnet34        | primary_heldout_architecture | CIFAR-100-LT            | ResNet34 CIFAR stem                     | matched-quality warmup steps fixed before scoring | at least 5  | no                     | no                       | GPU via Slurm     | results/e11_cifar100_resnet_condition_score_next/heldout_architecture  |
| primary_heldout_data_cifar10lt_resnet18      | primary_heldout_data         | CIFAR-10-LT             | ResNet18 CIFAR stem                     | matched-quality warmup steps fixed before scoring | at least 10 | no                     | no                       | GPU via Slurm     | results/e11_cifar100_resnet_condition_score_next/heldout_data          |
| escalation_imagenetlt_resnet50               | optional_scale_escalation    | ImageNet-LT-style split | ResNet50 or pretrained feature backbone | fixed before scoring                              | at least 3  | no                     | no                       | GPU via Slurm     | results/e11_cifar100_resnet_condition_score_next/imagenetlt_escalation |

## Acceptance Gates

| gate_id                        | scope                             | requirement                                                                                                                                                  | pass_condition                                                                                                                      |
|:-------------------------------|:----------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------|:------------------------------------------------------------------------------------------------------------------------------------|
| G1-no-target-leakage           | all held-out splits               | No target checkpoint, held-out architecture, or held-out data result can change score features, coefficient signs, hyperparameters, or reporting thresholds. | Protocol, score registry, split registry, and coefficients are committed before held-out result generation.                         |
| G2-primary-residual-prediction | P0 predictive condition claim     | The primary candidate must predict residual observed layer risk after removing the early-layer prior.                                                        | Held-out residual Spearman CI lower endpoint is above 0 on checkpoint, architecture, and data splits.                               |
| G3-threshold-direction         | matched-head-gain drift direction | The score must not only rank risk; it must preserve the below-one direction for candidate layers.                                                            | Below-one threshold accuracy is at least 0.8 on every primary held-out split.                                                       |
| G4-baseline-comparison         | condition-score usefulness        | The primary candidate must be reported against early_layer_prior, legacy_scaled_jvp_ratio, and source_observed_drift_positive_control.                       | If the candidate does not beat early_layer_prior on residual ranking, the paper must call the result a boundary or negative result. |
| G5-no-performance-overclaim    | paper wording                     | Passing this protocol does not justify final tail-accuracy or practical Muon optimizer claims.                                                               | Paper-facing text keeps the claim to local matched-head-gain tail-example drift unless a separate tuned benchmark passes.           |
| G6-reporting-completeness      | artifact review                   | Every held-out report must include seeds, checkpoints, tail accuracy before update, residual score metrics, threshold metrics, and all failures.             | Generated CSV, figure, and discussion artifacts exist and make e11-check validates them.                                            |

## Primary Readout

The primary readout is `condition_score_v2_calibrated_residual` on the three primary held-out splits. The paper may call the score predictive only if the held-out residual Spearman confidence interval lower endpoint is above zero, below-one threshold accuracy is at least 0.8, and the comparison against `early_layer_prior` is reported without cherry-picking.

## Leakage Rule

The legacy checkpoint-transfer tables may describe the failure that motivated this protocol, but they must not be used to adjust the primary candidate after this protocol is committed. Any new score after a failed held-out run must receive a new score id and a separate protocol revision.
