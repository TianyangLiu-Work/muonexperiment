# E11 Pasted Review Audit

This audit maps the latest pasted reviewer-risk note to the current repository state.
It is meant to keep the paper-facing claim boundary explicit and reproducible.

Carried-forward resolved issues from the previous pasted review are still covered by the current paper and validation gates: arbitrary-direction matched-gain definition; two-layer MLP blockwise spectral construction; local first-order approximation; Trajectory CIs; Synthetic point CIs; central condition should be written as a direct bound ratio; Layerwise diagnostics should use downstream-aware quantities; Squared drift ratio and RMS drift ratio must not be mixed.

| item | reviewer risk | current evidence | status | residual risk |
|---:|---|---|---|---|
| 1 | The one-step tail checkpoint may be too weak, so lower drift could mean preserving a mostly wrong tail function. | `main.tex` reports the checkpoint sweep over 20, 40, 80, 120, and 160 warmup steps and explicitly states that pre-update tail accuracy spans only `0 to 0.192` and positive-margin fraction spans only `0 to 0.192`. | addressed as a limitation, not solved as a stronger claim | A high-quality-tail checkpoint sweep is still missing; the paper should not claim protection of useful tail knowledge. |
| 2 | Full-logit drift may not be margin-relevant drift. | The one-step table and main text report centered-logit drift `0.5493 [0.5093, 0.5923]`, competitor-logit delta `0.6358 [0.5705, 0.7086]`, margin-delta `0.702 [0.6131, 0.8037]`, and true-class logit delta `1.68 [1.16, 2.431]`. | addressed with mixed readouts | The true-class logit readout is worse for spectral/polar; the supported claim is drift, not CE or margin improvement. |
| 3 | The synthetic experiment is aligned and should not be written as a general realized-drift predictor. | `main.tex` explains the aligned construction, reports point CIs as deterministic boundary checks, and adds an alignment ablation: with fixed positive spectra, randomized alignment gives observed squared drift ratio `1.171 [1.069, 1.283]` and spectral is lower in only `0.416` of 500 trials. | addressed | The rank condition is a worst-case bound-ordering condition; out-of-sample prediction remains unproven. |
| 4 | The synthetic model description must be a sandwich map, not `F(W;x)=Wx`. | `main.tex` uses `F_T(W)=B_TWA_T` and `J_T(D)=B_TDA_T`; the PDF text scan rejects stale `F(W;x)=Wx`. | addressed | None for the current synthetic description. |
| 5 | Multi-layer blockwise spectral direction needs a formal norm interpretation. | Appendix `Blockwise Spectral Geometry` proves a proposition for the max-over-block spectral norm, with dual sum-nuclear norm and blockwise polar maximizer. | addressed | This covers the two-layer bias-free MLP diagnostic, not arbitrary architectures. |
| 6 | Matched-head-gain should check actual head-loss linearization, not only tail-output linearization. | One-step results report actual head-gain relative errors: Fro/GD `0.02533 [0.02052, 0.03013]`, spectral `0.01662 [0.0148, 0.01845]`; rho sweep reports worst actual-head-gain relative-error upper endpoint `0.1176` at `rho/L_H=0.08`. | addressed for the tested local scales | The actual head-gain error increases at larger rho, so the paper should preserve the local first-order scope. |
| 7 | The default `rho=0.02 L_H` needs a step-size ablation. | The rho sweep covers five target fractions and keeps full and centered squared drift-ratio CI upper endpoints below 1; main text reports the worst full-drift ratio `0.5501 [0.5101, 0.5932]` and the largest head-gain error upper endpoint `0.1176`. | addressed | This is still a local digits diagnostic, not proof for arbitrary step sizes or training schedules. |
| 8 | Muon trajectory diagnostics may have state-selection bias. | The state-source control repeats the trajectory diagnostic on Fro/GD-generated states; Fro/GD-state `NS(M_t)` squared drift ratio is `0.7973 [0.7546, 0.8425]`, compared with NS-Muon-state `0.8019 [0.7583, 0.848]`. | addressed with one control | Random warmup checkpoint controls and larger trajectories are still missing for a full Muon-training claim. |
| 9 | The paper should report the head-alignment denominator and norm-specific scaling, not only drift ratio. | The one-step table reports head-alignment ratio `2.083 [1.991, 2.178]`, matched Frobenius-norm ratio `3.112 [2.976, 3.255]`, and matched operator-norm ratio `0.5543 [0.5395, 0.5695]`. | addressed | The safe wording is norm-specific: smaller operator-norm step despite larger Frobenius-norm step. |
| 10 | "Tail logits" is ambiguous. | Main text, captions, and two-page summary define and use `tail-example logit drift`, meaning drift of the full class-logit vector on held-out tail examples; validator rejects `tail-logit` PDF artifacts. | addressed | Future generated text should preserve `tail-example logit drift`. |
| 11 | The digits dataset needs enough reproduction detail. | `main.tex` states scikit-learn `8x8` digits, pixels scaled to unit interval, head classes `0--4`, tail classes `5--9`, per-class split, hidden dimension 32, noisy warmup, batch size, and held-out tail probe details. | addressed | It remains a controlled small-data diagnostic, not a standard long-tailed benchmark. |
| 12 | The fixed head/tail split may create a class-partition artifact. | The class-partition sweep repeats the diagnostic across five balanced 5-class head / 5-class tail partitions; main text reports the worst upper endpoint across partitions below 1. | addressed as a controlled ablation | Only five partitions are tested; larger datasets would still be stronger. |
| 13 | Absolute tail-loss table hides small deltas. | Appendix Table `Tail outcomes, margin-relevant drift, and head-gain readouts` reports pre-update tail CE plus Fro/GD change, spectral change, and spectral--Fro difference. | addressed | Small deltas should continue to be discussed as local diagnostics, not performance gains. |
| 14 | The 8-step forgetting probe needs a precise `rho_t` schedule. | Reproducibility details state that head mini-batches and reference targets are precomputed once from the initial checkpoint, with shared `rho_t=0.02 L_H(theta_0;B_t)` for both trajectories. | addressed | Realized nonlinear head-loss decreases are measured but not guaranteed identical. |
| 15 | The practical imbalanced-training diagnostic can distract from the main matched-gain story. | Main text keeps practical training out of the core evidence; Appendix labels it a non-matched-gain sanity check and reports LR sensitivity as a caveat. | mostly addressed | Appendix still contains practical-training numbers; they must not be used as evidence for the theorem. |
| 16 | Figure/table/PDF text artifacts hurt professionalism. | Validator scans LaTeX logs and PDF text for review headers, line rulers, undefined references, `exceedsssrank`, `¿`, replacement characters, compact CIs, and stale `F(W;x)=Wx`. | addressed | Benign vbox warnings remain from floats; visual inspection is still useful before external submission. |
| 17 | Recent references need conservative venue status. | Validator enforces conservative BibTeX: Davis/Chen/Spectra/Jiang/Muon-memory/Muon-ViT remain arXiv or OpenReview-style entries, `promo2026` remains `Submitted to ICLR 2026, OpenReview`, and no unverified ICML/PMLR proceedings claim is allowed. | addressed with automated guardrails | Final submission should still re-check public metadata near deadline. |
| 18 | The two-page version needs to stand alone better. | `two_page.tex` includes author info, setup details, matched-gain protocol, arbitrary-direction normalization, local-linearization note, claim boundary, and compact references. | addressed for a class report summary | It remains a dense two-page report, not a replacement for the full paper. |

Current validation gates:
- `python3 scripts/e11_validate_outputs.py`
- `python3 -m pytest tests -q`
- LaTeX serious-warning scan over `main.log`, `main.blg`, and `two_page.log`
- PDF text-layer artifact scan with Ghostscript
- `git diff --check`

Current paper boundary:
The supported result is a local, matched-head-gain function-drift mechanism.
The current evidence does not prove a general Muon optimizer theorem, a standard long-tailed benchmark improvement, protection of a high-quality tail predictor, or a general tail-accuracy improvement. The Fro/GD state-source control reduces one state-selection concern, but it remains a local diagnostic rather than a full trajectory distribution study.
